# SPDX-License-Identifier: Apache-2.0
[alias]
xtask = "run --locked --package xtask --"

[target.x86_64-unknown-none]
# R0 traps FP/SIMD instructions; runtime CPU detection cannot authorize them.
rustflags = ["-C", "relocation-model=static", "--cfg", "sha2_backend=\"soft\""]
