<!-- SPDX-License-Identifier: Apache-2.0 -->

# Current work state

Updated 2026-09-23. Replace this checkpoint rather than appending conversation history.

## Current foundation checkpoint

PR [#98](https://github.com/alseif0x/rustic-os/pull/98) merged as `38b2f1bd52af2b2cca0c543254077c24db101883`; local `main` was clean and fast-forwarded to `origin/main`. The current increment is on `codex/v7-range-read`, based on `38b2f1b`.

The owner authorized autonomous implementation decisions, versioned storage/protocol evolution, PRs and merges after verification (2026-09-22). Preserve #51 authority/recovery guarantees and never use the owner's real terminal volume for experiments. The production successor is v7, not a direct substitution of `Volume6` for `Volume`; v5/v6 remain frozen. Issue #51 is open and #52 remains untouched.

The selected #51 workload and budget are documented in [WORKSPACES.md](WORKSPACES.md): carry and launch a measured 215,296-byte native artifact plus its manifest, with a selected 256-object, 256 KiB/file, 64 MiB/volume and eight-record retention budget. Production remains v5-backed. Never experiment on the owner's real terminal data.

**Merged foundation:** `Volume7` provisions only fresh/disposable media; verifies dual-header mounts, checksums, whole-generation structure and live/retained payload CRCs; and implements version-pinned namespace queries, durable create/remove, tracked replacement, staged admission/execution/cancellation, and explicit retention maintenance. `upgrade_v5_to_v7` is a deliberate out-of-place converter that preserves supported scoped history or refuses unsupported history before target writes. v5/v6 implementation remains frozen.

**Current increment (`codex/v7-range-read`):** add ID-based, optional-version-pinned `Volume7::read_range`. It validates the live node before payload I/O, copies only intersecting extent sectors with one 512-byte buffer, and does not rescan the entire file CRC on every small range. Mount has already verified each live payload CRC; this method trusts the mounted owner's exclusive disk access and does not detect out-of-owner media changes after mount. A read I/O error may leave a copied prefix in the caller's buffer.

**Local evidence:** `cargo test -p rustic-fs --test volume7 --locked` passed 80 tests. Final-tree `cargo xtask check` passed formatting, workspace Clippy/tests, and host/guest-target builds/lints. Astra low independent review found no material findings; its identified coverage gaps (partial read error, oversized range at EOF, fenced owner, and post-replacement version pin) were added as tests. Host mocks do not establish device/DMA or guest behavior; no service/profile integration or consumer acceptance is claimed.

**Earlier evidence (v7 admission foundation, PR #96):** `cargo xtask check` passed 531 Rust tests across 93 test binaries, formatting, Clippy and host/guest-target builds ([log](../artifacts/workspace-v7-admission-check.log)); the focused v7 suite passed 61 tests. Tests cover exact command ordering, fragmented and empty payloads, retry mismatch/corruption, preflight refusal without I/O, cancellation/drain boundaries, pending-I/O fencing, all 105 admission publication cuts, all 103 execute/cancel cuts, and durable-head recovery after final-flush errors. `git diff --check` is clean. Astra low review found no material findings; requested barrier-order, final-flush recovery and too-late cancellation assertions were added. The authoritative PR-event checks all passed; the duplicate push run's measurement check was incomparable after one invalid injected sample. Host mocks do not establish real device/DMA behavior, v7 guest execution, service integration, bounded control latency or consumer workload acceptance.

DeepSeek returned HTTP 402 for exhausted API balance on the earlier admission increment; GPT-6 Luna Max completed that bounded implementation by explicit manual fallback. This is not automatic failover or a change to the DeepSeek default.

**Merged migration foundation (PR #97):** `upgrade_v5_to_v7` copies a mounted v5 source read-only to distinct disposable target media, preserving supported identity and scoped history or refusing unsupported history before target writes. It publishes payload, metadata and header with explicit barriers. `cargo xtask check` passed 531 Rust tests, formatting, Clippy and host/guest-target builds; the focused suites passed 18 migration and 61 v7 owner tests ([log](../artifacts/workspace-v7-migration-check.log)). This is host-mock evidence, not guest/service acceptance.

**Merged namespace foundation (PR #98):** `Volume7` now has stat/lookup/list plus durable create/remove with monotonic identities, candidate validation and copy-on-write publication. Tests cover more than 32 objects, full namespace and sequence/watermark exhaustion, root/system and refusal semantics, retained payload ownership, retries after deletion, pending-admission cancellation, and every publication write/flush cut for creation and removal with retained and unretained payloads.

**Earlier namespace evidence (PR #98):** `cargo test -p rustic-fs --test volume7 --locked` passed 73 tests; `cargo xtask check` passed formatting, workspace Clippy/tests, and host/guest-target builds ([log](../artifacts/workspace-v7-namespace-check.log)). Astra low review found no material implementation findings. These remain host-mock tests, not guest or device/DMA evidence. DeepSeek returned HTTP 402 before starting this implementation; GPT-6 Luna Max completed it by explicit manual fallback, not automatic failover.

**Next:** production remains concrete-v5 in `file-service`, ready advertises 32 objects/1 KiB files, and the app disk has no `PollDisk7` adapter. Build an explicit profile-2 service path while keeping v5 the default; avoid a broad backend abstraction. Then exercise consumer migration/guest acceptance for interruption, reboot/remount, corrupt input, bounded RAM and control latency. Keep #51 open until those criteria are evidenced.

## Prior foundation history

The entries below preserve earlier checkpoints. References to the branch or next action describe the state at that earlier date and are superseded by the current checkpoint above.

PR [#89](https://github.com/alseif0x/rustic-os/pull/89), tested head `5311dcb`, merged as `c077886` after its authoritative PR CI [35721138519](https://github.com/alseif0x/rustic-os/actions/runs/35721138519) passed all four jobs. Its publication-failure discipline is described below. The duplicate push workflow was cancelled, not counted as a passing check.

PR [#90](https://github.com/alseif0x/rustic-os/pull/90) merged as `0d789af` after all four jobs passed in authoritative PR CI [35723301684](https://github.com/alseif0x/rustic-os/actions/runs/35723301684); its tested and merged code trees agree. It fixes a compatibility defect without changing the format: receipt decoding confused the 256 live-object capacity with the maximum monotonic object ID. A real v5 create/delete sequence produces ID 257; after migration, a tracked write previously succeeded but remount rejected its receipt. The regression covers migration, tracked commit, durable remount and replay without additional writes/flushes, plus codec boundaries through `u32::MAX` and checksum-valid zero refusal. Both new tests fail with the original bound. Root `cargo xtask check` passed 433 Rust tests, formatting, Clippy and guest builds (`artifacts/v6-identity-check.log`); the independent image/migration oracle passed (`artifacts/v6-identity-oracle.log`). Astra low review found no material issues. The previous guest run below belongs to #89; no new guest execution is claimed for this decoder-only follow-up.

The owner explicitly authorized autonomous implementation and the decisions needed to continue, including versioned storage/protocol evolution, PRs and merges after verification (2026-09-22). The previous approval wait is resolved; do not request the same authorization again. Preserve the existing #51 scope, authority/recovery guarantees and real-volume protection. The production successor persists the identity watermark and scoped durable admission/content records rather than directly substituting `Volume6` for `Volume`. PR #91's codec foundation merged as `890befe`; local `main` is fast-forwarded and clean. Current branch for generation validation: `codex/workspace-generation-validation`, based on `890befe`.

Current increment: [format 7 and native payload profile 2](WORKSPACE-FORMAT7.md), including codecs and a read-only whole-generation validator. The new format fixes the persistent watermark, global sequences, authority-scoped retained records and immutable candidate extent snapshots; the new explicit wire marker distinguishes a `u32` size through 256 KiB from legacy messages. Neither is wired to production. No v7 mount, migration, service advertisement, SDK or new guest capability exists yet. Next: a disk owner that verifies aggregate checksums and payload integrity, calls the generation validator, and implements durable mount/publication with bounded staging and cancellation; then production integration and the full #51 failure/workload acceptance.

For PR #91, `cargo xtask check` passed 460 Rust tests, formatting, Clippy and host/guest builds (`artifacts/workspace-v7-check.log`); Python passed 263 tests (`artifacts/workspace-v7-python.log`); the legacy independent image/migration oracle passed (`artifacts/workspace-v7-oracle.log`). Its 27 new tests cover 23 filesystem and four ABI cases. Astra low review findings were corrected, including the previous-version invariant found by comparing against v5/ABI. Dedicated guards were mutation-checked. Final-head PR CI [35743259664](https://github.com/alseif0x/rustic-os/actions/runs/35743259664) passed all four jobs for `ada51d3`; PR #91 merged as `890befe`. Evidence covers codecs and legacy regressions, not new guest storage behavior.

Issue #51 was reopened on 2026-09-22 because its service and workload/failure acceptance remains unfinished. This branch validates a complete decoded v7 generation before mount: namespace roots/ancestry, retained identities and event sequences, monotonic per-object version observations and successful commit history, target chronology, and exact allocation-map ownership across live and retained payload extents. The validator is read-only and caller-supplied scratch keeps its working memory explicit. `cargo xtask check` passed 469 Rust tests, formatting, Clippy and host/guest-target builds (`artifacts/workspace-v7-generation-check.log`); `python3 -m unittest discover -s tools/tests` passed 263 tests (`artifacts/workspace-v7-generation-python.log`); `python3 tools/fs6_test.py` independently verified the 200,000-byte legacy image/migration case and refused eight damaged or inapplicable images (`artifacts/workspace-v7-generation-oracle.log`). Astra low review identified cross-record history contradictions; these were covered with locally valid negative and positive fixtures, and final review found no remaining material issue. This evidence is structural host validation and legacy regression coverage only: it does not establish v7 disk mounting, payload-byte checks, crash recovery, service integration or guest behavior. DeepSeek's worker route returned HTTP 402 insufficient balance; the authorized implementation fallback ran explicitly on GPT-6 Luna Max. No automatic model switching or default-route change was made. Follow with v7 disk mount/publication, bounded staging, service integration and the issue's disposable-volume guest acceptance.

## Development host and orchestration

Publication update: PR #88 merged the Ubuntu/orchestration work as `705f750`, including upstream `9dca326`. The integrated host checks passed 418 Rust and 263 Python tests. Main CI [35719179595](https://github.com/alseif0x/rustic-os/actions/runs/35719179595) passed all four jobs (check, boot, sandbox and measurements), establishing the integrated 24.04 guest/container checks. `gh pr merge --auto` merged immediately without waiting for those checks, so future PRs must explicitly observe successful checks before calling merge.

Historical foundation branch at PR #89: `codex/v6-failure-discipline`, based on `705f750`. The v6 failure discipline is implemented in separate mount, publication and payload modules: rollback allocation refusals, fence uncertain mutations/read/receipt paths, flush payload/inactive metadata before the header, and adopt the header only after its final flush. Remount flushes before decoding cached state, and a failed mount stays unusable. The format and public method signatures are unchanged; low-level public fields are not committed-state evidence. A torn single header sector can still produce `Corrupt`. This is a prerequisite of #51's interrupted-publication acceptance, not closure of the production file-service or guest-capacity gates.

Current validation: `cargo xtask check` passed 431 Rust tests, formatting, Clippy and guest builds (`artifacts/v6-failure-check.log`); Python passed 263 tests (`artifacts/v6-failure-python.log`); `tools/fs6_test.py` passed the independent 200 kB image/migration check and eight damaged/inapplicable-image refusals (`artifacts/v6-failure-oracle.log`). The new fault tests sweep 294 write/flush positions on each of two host device models. The worker reproduced failures on the original implementation and mutation-checked the mount barrier; Astra low review's missing recovery-durability test was added and re-reviewed with no remaining material findings. These are host fault-injection results, not guest fault-injection acceptance.

Guest validation on Ubuntu 26.04/QEMU 10.2.1: `python3 tools/boot.py run --mode block-user --timeout 90` passed both VMs (57.217 s write, 9.331 s replay), build `3425eba9ff571440`, against the working tree based on `705f750`. The 16 KiB artifact is read in four 4 KiB ranges; the receipt/version and whole volume digest agree across reboot, and the replay issues no additional writes/flushes. Evidence: `artifacts/v6-failure-guest.log`, `artifacts/boot/block-user/result.json` and serial logs. This is the direct probe, not production file-service integration. The increment is ready for its PR; wait for that PR's own final-head CI before merging.

The production owner remains `apps/file-server/src/startup.rs` plus `crates/file-service::Server`, both v5-backed. After publication failure handling, v6 still needs namespace lookup/ancestry and monotonic identity allocation, workspace/subject/service-instance receipt binding and content identity, bounded staged writes, admission/retention maintenance, and cancellable `PollDisk` publication. Preserve those contracts when wiring a v6 backend; a direct `block-probe` fixture does not establish production service integration.

Current host: native Ubuntu 26.04 amd64. PR #88 adds reviewed release-specific tool/firmware pins and selects them from the host's `/etc/os-release`; CI and the isolated Docker executor retain their 24.04 baseline. Local `environment.py verify` passes with QEMU 10.2.1 and OVMF 2025.11. Rebuild images before comparing environment metadata. This environment work does not advance or close #51.

The owner selected Opus 5 high orchestration and DeepSeek V4.1 Flash high implementation through Codex Router. Project defaults and the worker role now match that request; support roles retain Luna max and review retains Astra low. Fresh Opus CLI sessions loaded the profile and successfully delegated a bounded read-only task to DeepSeek. The current integrating task remained Astra; configuration changes do not switch a running task. See `docs/ORCHESTRATION.md` for exact IDs, evidence and the failed reverse-handoff probe.

Pre-integration checks on 2026-09-22, based on `d2f2476` plus the environment changes: `cargo xtask check` passed (418 Rust tests, formatting, Clippy and guest builds), and `python3 -m unittest discover -s tools/tests` passed (214 tests). Logs: `artifacts/environment-check-20260922.log` and `artifacts/environment-python-20260922.log`. An Astra low independent review found no material issues. `python3 tools/boot.py test --timeout 45` completed with exit 0 and all 22 scenarios verified, including native recovery's 49 cases and 98 VM boots; evidence is in `artifacts/environment-boot-20260922.log` and `artifacts/boot/suite.json`. These guest results are from Ubuntu 26.04/QEMU 10.2.1 and do not establish the later integration of `9dca326`; that revision's evidence is identified above.

## Direction and current increment

Own modular Rust OS; isolated native applications, human and agent clients sharing authority and semantics. MCP is optional. Multiple preempted processes currently execute on one CPU; SMP, independent application installation and physical-machine support remain unimplemented. [Issue #1](https://github.com/alseif0x/rustic-os/issues/1) and the [systems roadmap](architecture/systems-roadmap.md) own sequencing.

Current user-directed increment on `b73d266` (PR #86): [explicit boot harness evidence](JEV-HARNESS.md) for D1. The suite exports its existing acceptance per completed mode, bound to original result and serial bytes, with a fresh suite identity and stale-report cleanup. The boot importer accepts an optional explicit harness attachment and promotes validated acceptance without changing v1 classification or prior scores. CI exercises the offline adapter against actual deliberate panic/hang controls; no provider calls or credentials are added. Producer code uses only the standard library and does not impose research input caps on native artifacts. The independent review's tuple/JSON association finding was fixed by strict JSON normalization while preserving raw source hashes. Local validation: 418 Rust tests plus formatting/Clippy/builds and 249 Python tests passed; final-head native CI evidence is recorded in this increment's PR. This supplies collection infrastructure and native controls, not a new representative failure dataset or semantic service. The owner requested stopping after this increment; no subsequent phase is started. #51 remains the foundation priority.

Previous D1 increment (PR #86, `b73d266`): native boot/sandbox/GitHub-job evidence import and a frozen historical D1 evaluation, following [JEV-PLAN.md](JEV-PLAN.md). The importer preserves original JSON and source hashes, promotes validated facts and never executes report commands or infers harness acceptance. Eight selected historical observations (seven failures, one passed control) were source-checked by independent review before inference. JEV accepted 5/7 failure diagnoses, all five matching the observed-family labels, versus local signatures 1/6 accepted correct; it abstained on two failures and the passed control. The control still received a raw timeout proposal at confidence 0.43, so this is not proof of deterministic pass handling. Eight calls cost USD 0.001722714. [Retained evidence](evidence/jev-triage-historical-v1.json) records accounting, frozen hashes and complete live reports; [the guide](JEV-REAL-EVIDENCE.md) records provenance and limitations. No classifier, prompt, threshold, dependency, guest, kernel, ABI or authority changed. `cargo xtask check` passed 418 Rust tests plus formatting/Clippy/builds; the separate Python suite passed 234 tests. Duplicate native reports are rejected even after case renaming. Decision: keep explicit research mode; seven selected failures do not meet the representative usefulness gate. Next D1 acceptance: collect prospectively labelled failures and explicit harness acceptance, then evaluate separately versioned refinements without tuning on this retained sample. #51 service integration remains the foundation priority; D2–D4 and native semantic services are not implemented.

Previous D1 increment (PR #85, `22bd0e8`): [portable failure triage](JEV-TRIAGE.md) and compatible shared native Decisions transport. The first 50 synthetic live cases cost USD 0.003946194; on 20 reserved cases JEV accepted 17 hypotheses, 15 correctly, versus local rules 14/15. [Synthetic measurement](evidence/jev-triage-v1.json) remains unchanged; promotion was deferred because the 90% gate was unmet and engineering fixtures do not establish real-world usefulness.

Previous foundation increment (merged in `aed366c`, PR #83): **the deliberate v5 to v6 volume upgrade for [#51](https://github.com/alseif0x/rustic-os/issues/51)**. `crates/fs/src/upgrade6.rs` reads a mounted v5 volume, refuses the two states it cannot reconstruct (a retained v5 recovery record, whose record carries the original file bytes as evidence; a v5 provisioning envelope with a different lineage) and otherwise copies identity, names, versions, kinds, spaces and file bytes into the v6 layout, reporting the source sequence, the v5 identity watermark `next` (v6 does not carry an allocator) and the files/directories/bytes moved. The two layouts overlap, so payload is staged into the free v6 region (sector 205 and above, outside the v5 layout) before one publishing flush overwrites sectors 8..205; until that flush the v5 header is intact, and a staged failure therefore leaves a mountable v5 volume. A publish torn between the structure writes and the header write still leaves the old v5 header describing data the structure writes have already overwritten, so migration is documented as one-way and needing a caller-owned backup. The v5 second metadata bank (sector 13) sits outside the v6 structures, so the upgrade clears it before publishing: otherwise a v5 mount could still find that bank and publish a v5 header over the v6 one. `Volume6` gained an in-memory `blank` constructor and a `stage_bytes` split out of `write_file` so the migration preserves the version it read instead of bumping it, and `Node6::name()` was added. Seven host tests cover preservation (including a deleted identity that must not return and exact free-sector accounting), both refusals, a staging failure, a torn publish, exactly one mountable layout and an unformatted disk; `crates/fs/tests/fs6_image.rs` runs the provision and the migration against a real image file of full volume size and exports the images for the independent reader that the next step adds.

The same increment adds that reader and closes the three defects it exposed. `tools/terminal_support/oracle6.py` reads a v6 image from the format description with Python's own CRC, and `tools/fs6_test.py` (also a CI step) has `cargo test -p rustic-fs --test fs6_image` export the images, verifies them with the reader, and compares the migration result against the existing v5 reader's view of the source image; it records six damaged images the reader refuses and notes that payload bytes carry no checksum, so a flipped payload byte is caught by the consumer's comparison rather than by the format. What it exposed: `Node6::decode` accepted a `name_length` past the 32 bytes it holds (a panic on media this code did not write) and a record claiming more than the 512-sector per-file cap, and `Volume6::stage_bytes` never released the runs a rewritten file replaced, so every rewrite leaked the old payload (a 100-byte rewrite of a 100 kB file held 197 sectors instead of 1). `decode` refuses both malformed records now, `stage_bytes` releases the replaced runs after the new payload exists, and the reader checks the free-space map against the live records so a leak cannot pass again.

The step after that makes the layer executable outside a test: `tools/volume` (package `rustic-volume`, host-only, depends on `rustic-fs`) exposes `provision <image> <lineage>`, `seed <image>` for a small v5 experiment volume, `migrate <image> <lineage>` for the one-way upgrade and `report <image>` for a JSON view of a mounted volume. Images are refused unless they are exactly one volume long, so a truncated image cannot read as a volume, and the receipt table gained a `lineage()` accessor so the report can state the volume's identity. `tools/fs6_test.py` now drives that tool and cross-checks its report against both readers, in addition to the exported 200 kB image.

The latest step gives the v6 layout operation identity: `Volume6::write_tracked` refuses a full table, a stale epoch, a foreign lineage and a version conflict before staging anything, then writes the payload, bumps the version and retains the receipt in one commit, so a replay of the same retry repeats the identity instead of the work and a conflicting retry is `IdempotencyConflict`. Five host tests cover the commit, the replay (no disk operations, unchanged version), the conflict and expiry paths, a full table refused before any payload is staged, and a failed commit publishing neither data nor receipt. `crates/fs/tests/fs6_image.rs` exports a tracked image and the independent reader now checks the binding a receipt claims: the identity is still a live file and `previous < committed`; it also fixes the reader's own receipt-id parse, which no earlier fixture had exercised. The next step adds bounded range reads: `Volume6::read_range` streams only the extents a byte range touches (whole-file `read_file` is the special case), refusing a range that starts past the end and copying the remainder of one that runs past it, so a guest can read an artifact larger than its free RAM without materialising it.

That range API made a guest read possible. `Volume6::read_range` is in `main`, and the `block-user` boot mode now runs a fifth `block-probe` role (14): the guest mounts a host-provisioned v6 volume at sector 1024 of the disposable disk **in place** (the node table is larger than its 64 KiB stack, so `Volume6::mount_into` fills a process static instead of returning the table by value), checks the artifact's identity and length, reads 16 KiB in four 4 KiB ranges, folds a 64-bit FNV digest and writes it to the sector before the volume. `tools/boot_support/workspace_evidence.py` writes that volume with `rustic-volume`, re-reads it with `terminal_support.oracle6` and recomputes the digest from its own artifact, and `tools/boot_support/block_user_evidence.py` requires the `RUSTIC WORKSPACE` line and five launched applications. The mode has a documented 90 s floor (`MODE_FLOOR` in the runner) because the v6 mount alone costs 96 real block requests; the artifact is 16 times the v5 file limit and the guest buffer is 4 KiB. On the first boot the guest also creates a file whose bytes are that digest and publishes it with `write_tracked`, so its data, version and receipt commit together; the second boot must find the receipt and the bytes, verify the version the receipt names and replay the same retry with no additional writes or flushes counted by the guest disk adapter, and the host requires the volume digest to be identical across the two boots. This is the first guest execution of the v6 layout and the disposable-volume acceptance of a large artifact read, written, rebooted and replayed independently.

Earlier increment `800ee7b`: **the frame-address budget raised to 16 GiB**, the follow-up to closed #48. `LIMIT` in `kernel/src/arch/x86_64/memory/physical.rs` is now 16 GiB, so the two static frame bitmaps are 512 KiB each and the metadata cost is 1 MiB; a machine whose usable map reaches past it is still rejected before import rather than silently reduced, and 3/6 GiB guests that previously panicked at `Frames(AddressLimit)` now boot; `FrameAllocator::address_limit()` reports the storage bound instead of the owner hardcoding the old one. The memory fixture counts, writes, reads, releases and re-verifies real frames above the old 1 GiB boundary (`kernel/src/arch/x86_64/memory/tests/{allocation,mod}.rs`). The boot report adds `limit_bytes`, `high_boundary_frame`, `high_frame` (highest frame index taken there) and `high_frames` (how many) to `RUSTIC MEMORY` and a new `RUSTIC MEMORY_FRAMES usable_bytes/managed_bytes/reserved_bytes/allocated_frames/free_frames` line. `-m` is part of the image identity: `tools/boot_support/image.py` records it and gives each non-reference profile its own `artifacts/boot/<mode>-<mib>` directory, `tools/boot_support/runner.py` reads it, `tools/boot.py run --memory` selects any 256 MiB step up to the 16 GiB budget (refused where a harness pins 256 MiB), `tools/boot_support/scenarios.py` checks both memory lines, and `tools/memory_profiles_test.py` boots 256/512/2048/4096 MiB by default and ties the recorded `-m` and usable band to the profile. A panic that cannot take the `Serial` token now reports its reason through `arch::panic_bytes` instead of exiting silently (`kernel/src/diagnostic.rs`); that raw branch has no fixture of its own, and `fatal()`/the boot `MAP` line can still exit silently when the token is held.

Previous increment `a7787c8`: **#49 completed with a product consumer and shell-observable exhaustion**. The `TASKS_OWNER` child in `apps/utility` keeps its candidate bytes in one SDK `Heap` page reserved at the first `TASKS_EDIT`/`TASKS_BEGIN` of a plan and released when the apply concludes or the state clears (`apps/utility/src/tasks/{state,owner}.rs`, borrowed-storage `Candidate<'a>`/`CandidateBuilder<'a>` in `crates/tasks-client`, code `106` for a memory refusal); `act PID tasks-heap-stress` (actor 31, TASKS_OWNER only) grows a private heap to the budget, releases it and reports `[error, peak, full, pages_after_release, limit]`; the supervisor forwards `INFO` word 7 and the shell `mem` line ends with `heap_pages=`; the supervisor's actor admission predicates live in its library with host tests, and the shell acceptance lifecycle cases compare heap pages before and after.

Earlier increment `e17f6b4`: **dynamic user memory with a bounded native SDK allocator** for [#49](https://github.com/alseif0x/rustic-os/issues/49). Three ordinary process syscalls in `crates/abi/src/memory.rs` (`MAP` 21, `UNMAP` 22, `QUERY` 23) map zeroed, never-executable pages inside a per-process heap window (`0x1000_0000`, 128 pages, 64-page budget) whose reservation lives in the pure `kernel/src/process/heap.rs` bitmap; `map_user_pages`/`unmap_user_pages` sit over the existing zeroed mapping primitives with multi-page rollback, ELF segments may not reach the window, reap reclaims through the existing `destroy_user`, and `INFO` word 7 sums heap pages of unreaped processes. The SDK adds `memory::{raw, allocator, Heap}`: typed syscalls, a first-fit allocator with in-region three-word headers, coalescing, generation-tagged handles and exact accounting (`Region` is its only `unsafe`), and a `Heap` that grows and shrinks by whole pages; no `GlobalAlloc`. The consumer is `apps/sdk-probe` under the kernel `ok` fixture. [Process ABI](PROCESS-ABI.md), [Memory](MEMORY.md) and [SDK](SDK.md) document the contract and limits.

Earlier increment `4458b73`: the second native semantic tasks client (`apps/utility` under `TASKS_OWNER` over `crates/tasks-client`, two-scope grants, deterministic cuts) with its guest suites `tools/tasks_owner_test.py`, the `tasks.owner` phase of `tools/terminal_test.py` and `tools/tasks_owner_normal_test.py`; see [Tasks](TASKS.md) and [Authority](AUTHORITY.md).

## Verified evidence

Current harness increment: `cargo xtask check` passed 418 Rust tests, formatting, Clippy and builds (`/tmp/rustic-harness-cargo.log`); `python3 -m unittest discover -s tools/tests` passed 249 tests (`/tmp/rustic-harness-python.log`). New host contracts cover stale/partial reports, large native logs, result normalization, source association and true/false acceptance guards; CI additionally checks the adapter against actual panic/hang controls without inference. Earlier historical live reports remain in `docs/evidence/jev-triage-historical-v1.json`. The guest evidence below belongs to earlier foundation increments and is not a native JEV claim.

This increment's evidence (working tree): `python3 tools/memory_profiles_test.py` records 256 MiB usable 213737472 with `high_frames=0`; 512 MiB usable 482172928 with `high_frames=0`; 2048 MiB usable 2092781568, 510774 managed frames, `high_frames=252847`; 4096 MiB usable 4240257024, 1035060 managed frames, `high_frames=777133`, all `metadata_bytes=1048576`, `limit_bytes=17179869184`, full `ok` fixture success. A 6 GiB guest (not part of the default suite) reports usable 6388 MiB and 1301419 frames above the old limit. An 8 GiB guest reaches usable 8535207936 and 1825705 high frames, then fails the process fixture's progress assertion (`kernel/src/process/runtime/tests/execution.rs:12`) because the loaded host cannot give the spinner enough time; the memory evidence for that profile is kept in `artifacts/memory-profiles/ram-8192-host-limited.log` as a host limit, not a kernel result.

`cargo xtask check` passed: 378 Rust tests (the `xtask` command runs only cargo tests), fmt, Clippy `-D warnings`, kernel builds and kernel-target clippy, guest clippy for every app. `python3 -m unittest discover -s tools/tests` is a separate run of 195 host Python tests, not a subset of those 378. New tests: the two memory report lines (budget-derived metadata cost, high-frame count upper bound, frame split), the declared profile set and the pinned-harness refusal in the runner, and one image directory per non-reference profile.

`python3 tools/boot.py test --timeout 45` verified all 22 boot scenarios on the final tree, including the `ok` memory fixture, every `memory-*` fault, the block modes and the native recovery suite (49 cases, 98 VM boots); see `artifacts/boot/suite.json` and `artifacts/memory-profiles/boot-suite.log`. The reference suite stays at 256 MiB; `block-user` needs its documented 45 s.

`python3 tools/memory_profiles_test.py` (three boots, build ids `bf83f282b215c6f3`/`421a70b5500ec62e`/`38f48ad707ba3c29`) records: 256 MiB `usable 214659072`, 52248 managed frames, 15 table frames, `high_frames=0`; 512 MiB `usable 483094528`, 117784 managed frames, 15 table frames, `high_frames=0`; 2048 MiB `usable 2093703168`, 510999 managed frames, 16 table frames, `high_frames=253072`, `high_frame=515867`, boundary-to-highest span 253724; every profile `metadata_bytes=131072`, `limit_bytes=2147483648`, reserved `651264` bytes, `exhausted=free_before=free_after`, usable memory inside its profile band, the recorded QEMU command asking for the profile's `-m`, and full `ok` fixture success (8.7/9.2/31.6 s on the run recorded at `faaf640`; the 2048 MiB boot varies several-fold with host load, so the suite's 180 s budget is the relevant bound). The suite derives the count from the span and the map's small hole above the boundary (652 frames of firmware regions), so reporting the span as the count fails it; exact frame counts shift by a frame or two with the embedded kernel image, so only the invariants are asserted. `evidence.json` records the revision and worktree it was produced from plus each profile's image directory and digest, so the artifact cannot be read as a later tree. The stock kernel before this change panicked at 2048 MiB with `memory initialization: Frames(AddressLimit)` after `RUSTIC MAP entries=32 usable_bytes=2093776896`; that negative is kept in `artifacts/memory-profiles/stock-2048-panic.log`. Evidence `artifacts/memory-profiles/evidence.json`, `run.log`, `boot-suite.log`, `ok-{256,512,2048}/`, plus each profile's own image metadata in `artifacts/boot/ok`, `ok-512`, `ok-2048` (the 256 MiB reference profile owns the bare `artifacts/boot/ok`). The acceptance summary was posted to [#48](https://github.com/alseif0x/rustic-os/issues/48#issuecomment-5698045655) and the issue was closed with commit `800ee7b`; a correction for the off-by-one counts and the review follow-ups is posted as a second comment, and the follow-ups themselves are commit `c3727a8`.

Previous increment evidence for `a7787c8` stands: acceptance build `6fd99ca5ffb118ef` for `tools/tasks_owner_test.py` and the `tasks.owner` phase of `tools/terminal_test.py`, and ordinary build `c8e2c0758cfe70fd` for `tools/tasks_owner_normal_test.py`, in `artifacts/tasks-owner-test/tasks-owner.json`, `artifacts/terminal-test/terminal.json` and `artifacts/tasks-owner-normal/evidence.json`. The kernel is otherwise unchanged since `e17f6b4`, whose `--mode ok`, `block-user` and `recovery-test` evidence stands; the `ok` memory line changed shape, so those stored serial logs no longer match the current parser and must be read as historical.

## Next acceptance

**#50 is closed** (`1358253`): decision, bounded-transfer measurement (960 bytes, 24 operations of 40 bytes, write 0.27 s, read 0.079 s), control latency under load, queue memory (the manager that owns the queues measures 9,816 bytes) and the stopped-reader saturation case are all recorded in `docs/BULK-TRANSFERS.md` and in the issue. Next in the roadmap: **#51** (application workspaces beyond the bootstrap file and receipt limits) and then **#52** (launch trusted native applications without rebuilding the kernel).

**#51 current progress:** the v7 owner provisions and validates mounts, supports tracked replacement, durable staged admission/execution/cancellation, and explicit safe retention maintenance. `upgrade_v5_to_v7` copies a source read-only to a distinct disposable target, preserving supported scoped history and refusing unrepresentable history before target writes. The focused sparse-disk suites pass 18 migration cases and 61 v7 owner cases; `cargo xtask check` passes formatting, workspace Clippy/tests and guest-target build/lints ([log](../artifacts/workspace-v7-migration-check.log)). The migration cases include every publication write/flush failure and a checksum-valid, equal-CRC byte-mismatch refusal. This is host-mock evidence only, not device/DMA or guest/service acceptance. Still owed: production service/profile integration and consumer migration/guest acceptance for interruption, reboot/remount, corrupt input, bounded RAM and control latency. #52 is untouched. The issue remains open because those acceptance conditions remain incomplete.

**#50 history: the A1 payload growth is implemented but parked on `archive/bulk-a1-payload-2026-09-20` (commit `7b6434c`), not merged.** It passes `cargo xtask check`, `ok` and the block modes, but breaks the native terminal boot: the supervisor's mount job fails at the shell-connect step, the kernel refuses with `ipc::Error::Handle` (a peer is not in a live state) or `ipc::Error::Quota` (no channel or per-owner handle left) non-deterministically, and the shell is left without a file binding so every file command answers `Unavailable`. Retrying the connect 500 times did not help. Two independent findings are recorded on that branch: the broker's connect rollback replaced the refusal that caused it with its own cleanup error, and the supervisor syscall collapses every broker refusal into `Error::Full`. #50's measurements are now on this trunk (`19dd175`, `e96c22c`): a bounded 960-byte transfer (24 operations of 40 bytes) measures write 0.27 s and read 0.079 s end to end, and control progress under load measures 0.069 s under write pressure, 0.84 s worst case against a 2 s tripwire while a service is stalled, 0.21 s interrupt during pending I/O and 0.021 s revocation. Still owed before #50 closes: memory per transfer slot and queue measured rather than estimated, and a saturation case with a stopped reader. The A1 blocker is confined to the parked branch and does not block the rest of the tramo.

#48 is closed at `800ee7b`; its acceptance bullets are evidenced for the three declared profiles, and physical maps larger or sparser than 2 GiB stay rejected and belong to a bounded follow-up. Known limits to carry: the budget is a static storage constant, so a larger machine needs a reviewed increase or a different representation; per-profile evidence is QEMU q35/TCG only and no physical machine is claimed; `high_frames` comes from one exhaustive walk, not from a targeted per-frame API; `reserved_bytes` is `usable − managed` and therefore includes partial-page rounding slack as well as reserved frames; the stored #20 budget for `allocator_metadata_bytes` is still 65,536 B, so comparing a current build reports a regression by design until a re-baselined measurement is recorded; #53 must review the enlarged static `unsafe` bitmap owner before mapping or runtime interfaces spread.

Keep [#22](https://github.com/alseif0x/rustic-os/issues/22) open for general discovery and complete mission acceptance. Keep #47/#43 correctness work finite. Optional file-service semantic/profile expansion remains frozen. #50 is closed; #51 is active and #52 remains untouched. Review #53 concurrency ownership before expanding runtime assumptions. #54/#37 and #55 remain bounded probes; #56 research does not change the adopted kernel model.

## Branch consolidation (2026-09-20)

The audit found 26 remote branches: `main`, 22 branches whose trees exactly match their merged PR commits already in `main`, the pending guest write/replay PR #83, and two parked A1 branches. PR #83 carries the guest replay write/flush counter check added during review. Archive tags `archive/bulk-a1-implementation-2026-09-20` (`a92707a`) and `archive/bulk-a1-payload-2026-09-20` (`7b6434c`) preserve the superseded plan and failed experiment; neither is current implementation guidance or merged into `main`. The latter tag includes the former commit. Recover the experiment with `git fetch origin --tags` and `git switch -c bulk-a1-investigation archive/bulk-a1-payload-2026-09-20`.

Validation for the replay-counter correction: `cargo xtask check` passed (418 Rust tests, formatting, host/guest Clippy and builds), and `python3 -m unittest discover -s tools/tests` passed 200 tests. GitHub runs `35539017737` and `35539015993` passed checks and measurements but their boot jobs exhausted the old 15-minute job cap during recovery acceptance. The workflow now allows 30 minutes for the full boot and isolated-execution jobs; individual harness timeouts and acceptance assertions are unchanged. Final-head CI [35539899402](https://github.com/alseif0x/rustic-os/actions/runs/35539899402) passed all four jobs; PR #83 merged as `aed366c`, and the audit ended with only local and remote `main` plus the archive tags. The local host has QEMU 10.2 rather than the pinned 8.2, so local VM execution is not claimed.

## Workspace constraints

Never experiment on `artifacts/terminal/data.raw`. Preserve any owner modification of `LICENSE`; do not stage it incidentally. Commits are authored as the owner (`alseif0x`) with no AI attribution. Maintained docs remain English. The owner authorizes implementation decisions, commits, pushes and merges within the agreed project scope, including necessary versioned architecture/ABI evolution. Preserve authority, recovery and data-safety guarantees; record decisions and dependency provenance rather than treating authorization as evidence. Each increment uses its own branch and PR with verified evidence and known limits. Explicitly observe all authoritative final-head PR jobs passing before merging that exact head; do not enable early auto-merge. Ask only when missing information, a conflict or action outside the agreed scope genuinely requires owner input, not to repeat granted authorization. On Ubuntu, activate Rust with `source ~/.cargo/env` when Cargo is not on PATH. Historical memory-profile measurements above came from WSL on a memory-constrained Windows host; this checkout now runs on native Ubuntu 26.04. Re-measure before carrying those host limits over to this machine.
